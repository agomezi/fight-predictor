# Pinned exactly so two machines resolve to the same environment. The project's
# results are measured to ~0.001 accuracy, and a library version change can move
# a number by more than that -- an unpinned install makes runs incomparable.
# Verified together on Python 3.14.7, all suites passing.
pandas==3.0.5
numpy==2.5.3
scikit-learn==1.9.1
scipy==1.18.1
